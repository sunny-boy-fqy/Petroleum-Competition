# v4 云端训练环境 —— 依赖清单
#
# 基础栈（镜像**预装**，不得变更、不得 pip 替换；torch 由镜像提供）
#     CUDA 12.8（驱动能力）/ Python 3.11 / PyTorch 2.7.1+cu128
#     注意：torch 禁止 pip install（见 PLAN.md §3.3.1 安装纪律）。
#     本文件里的 torch 行只作**事实记录**，setup_deps.sh 会显式跳过它。
torch==2.7.1+cu128
numpy             # 版本不钉死：基础镜像通常自带（torch 的 wheel 不依赖 numpy，故需显式安装）

# 额外轻量包（required；允许 `pip install --no-cache-dir`，装完 `pip cache purge`）
# R5-M1：不写死小版本 —— 镜像/python 组合会变，钉死会在升级时误报；
#        精确版本以 `versions/locks/cloud_frozen.txt`（云端 `pip freeze` 回填）为准。
pandas
scipy
scikit-learn
einops

# 可选（缺失时自动降级，不阻塞训练；见 src/portability.py 与 check_env.py::degraded_paths）
# tensorboard   # 平台"迭代曲线"观测；缺失时只用 JSONL
# pyarrow       # 仅当改用 parquet 分片时才需要（当前分片是 .npz，无需）
# onnx
# onnxruntime

# 明确不安装（磁盘与版本风险）
# matplotlib / jupyter / wandb / torchvision / timm
# flash-attn / xformers / apex / deepspeed（需现场编译 CUDA 扩展）
