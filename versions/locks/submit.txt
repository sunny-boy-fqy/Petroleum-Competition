# v4 提交推理环境（评测机应具备同一套预装镜像）
# CUDA 12.8 / Python 3.11 / PyTorch 2.7.1+cu128
# 推理主路径：torch.load(map_location='cpu', weights_only=True) + 纯 numpy 后处理
#   —— torch>=2.6 起 `weights_only` 默认为 True；本项目 checkpoint 只含张量与
#      原生 python 标量，因此无需放宽该开关（见 PLAN.md §3.3.3 迁移注意）。
# 兜底路径：ONNX（onnxruntime）或 .npz 权重清单
torch==2.7.1+cu128
numpy             # 不钉死版本：随镜像 torch 提供

# 推理侧只需要 numpy；pandas 仅用于把提交结果整理成表格（缺失也能提交）
# pandas

# 可选兜底（缺失时自动降级，见 PLAN.md §3.3.2 portability 层）
# onnx
# onnxruntime

# 明确不需要：scipy / scikit-learn / einops / pyarrow（训练专属）
