# 基于测井曲线的储层参数预测 —— 赛事规则说明

## 一、业务背景

油气勘探开发过程中，孔隙度（POR）、渗透率（PERM）、含水饱和度（SW）等储层参数是储层品质评价、油气储量计算和开发方案制定的核心参数，广泛应用于资源评价、储量计算、开发部署和动态调整等关键业务环节。

测井技术通过在钻孔中连续采集地层的电学、声学、放射性等多种物理响应信息，形成高分辨率测井曲线，为储层参数的定量评价提供了核心数据支撑。基于测井数据准确获取储层参数，是实现油气勘探开发精细化、智能化的关键基础。

## 二、问题与挑战

现阶段储层参数计算仍以传统物理模型和经验公式为主，存在三方面局限：

1. **人工依赖程度高**：计算流程需要地质与测井人员反复调参校正，流程繁琐，难以支撑大规模井网快速评价；
2. **场景泛化能力弱**：面对强非均质性、岩性突变等复杂地层，不同区块预测精度波动明显；
3. **数据价值挖掘不足**：测井数据维度丰富，传统方法难以充分挖掘多曲线间非线性关联。

针对上述问题，可通过人工智能方法构建数据驱动的预测模型，减少人工干预，提升复杂地层场景下的预测稳定性，充分挖掘多测井曲线的关联信息，全面提升储层参数计算效率与精度。

## 三、算法任务描述

参赛队伍基于官方提供的真实测井曲线数据集搭建模型，对各深度点三类储层参数进行**回归预测**：

- **输入**：多维测井曲线；
- **输出**：每个深度点对应的孔隙度（POR）、渗透率（PERM）、含水饱和度（SW）连续数值。

**晋级要求**：本赛题基线为 **75 分**，最终 B 榜综合得分必须超过该分数线，方能具备晋级决赛、参与奖项评选的资格。

## 四、赛制设置

初赛规则、决赛规则等赛制设置详见【赛事中心 - 赛事整体说明】。

## 五、数据集说明

### 5.1 数据集划分

按赛事说明，数据集为 **90 口训练井 + 10 口测试井**，文件统一以“井次ID.txt”命名。**本地实际下发的 data 目录为 80 口训练井 + 10 口测试井**（以实际文件为准）。

**文件格式（本地已验证）**：每个文件共 3 段——第 1 行为曲线名称（表头），第 2 行为单位行，第 3 行起为逐深度采样数据（逗号分隔，0.1 m 采样间隔）。

| 数据集 | 井数 | 列数 | 内容说明 |
| --- | --- | --- | --- |
| 训练集 `data/train/` | 80（规则描述 90） | 17 列（14 输入 + POR、PERM、SW） | 含输入曲线与输出曲线 |
| 测试集 `data/test/` | 10 | 14 列 | 只有输入曲线，无输出曲线 |

### 5.2 测井曲线字段说明（按数据文件列顺序）

字段释义以 [`data/参数说明.txt`](file:///d:/基于测井曲线的储层参数预测/data/参数说明.txt) 为准，单位行摘自各井数据文件（不同井间单位写法略有差异，如 GR 为 `API`/`mg`、CNL 为 `%`/`p.u`，解析时建议只保留表头与数据行、跳过单位行）。

| 列序 | 参数名 | 参数说明 | 单位 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | DEPTH | 深度 (Depth) | m | 采样定位基准 |
| 2 | GR | 自然伽马 (Gamma Ray) | API / mg | 岩性识别、泥质含量计算 |
| 3 | PE | 光电吸收截面指数 (Photoelectric Factor) | b/e | 岩性识别 |
| 4 | SP | 自然电位 (Spontaneous Potential) | mV | 岩性识别、渗透性判断 |
| 5 | CAL | 井径 (Caliper)，测量井眼实际直径 | cm | 井眼扩径影响校正 |
| 6 | AC | 声波时差 (Sonic Transit Time) | μs/m | 孔隙度计算 |
| 7 | DEN | 密度 (Bulk Density) | g/cm³ | 三孔隙度测井之一 |
| 8 | CNL | 中子孔隙度 (Compensated Neutron Log) | % / p.u | 三孔隙度测井之一 |
| 9 | RXO | 浅探测电阻率 (Shallow Resistivity) | Ω·m | 探测冲洗带 |
| 10 | RT | 深探测/真电阻率 (Deep/True Resistivity) | Ω·m | 探测原状地层，评价含油性 |
| 11 | DEVI | 井斜角 (Deviation Angle) | ° / DEG | 井身结构 |
| 12 | AZIM | 方位角 (Azimuth Angle) | ° / DEG | 井身结构 |
| 13 | BIT | 钻头直径 (Bit Size) | none | 井身结构 |
| 14 | CASE | 套管直径 (Casing Size) | none | 常为 -99999 占位 |
| 15 | POR | 孔隙度 (Porosity)，连续型变量 | — | **目标参数** |
| 16 | PERM | 渗透率 (Permeability)，连续型变量 | — | **目标参数** |
| 17 | SW | 含水饱和度 (Water Saturation)，连续型变量 | — | **目标参数** |

### 5.3 数据质量说明（本地实测，建模前必读）

训练集共约 **73.0 万**采样点，标签分布如下：

| 标签状况 | 采样点数 | 占比 | 说明 |
| --- | --- | --- | --- |
| POR = PERM = SW = -99999.000 | 6,572 | 0.9% | 缺测标记（本地以 NaN 处理） |
| 常量占位（POR=0.100 / PERM=0.010 / SW=99.900） | 487,225 | 66.7% | **不删除，作为联合常量状态参与全量评分** |
| 其他（疑似有效） | 236,471 | 32.4% | 有效标签 |

**项目决策（2025-06-12）：常量占位 (POR=0.1, PERM=0.01, SW=99.9) 不剔除。**

- 该项不是官方文档；其他及早期版本中"应剔除"的表述仅为部分本地分析的初步建议，并非赛事官方评测规范。
- 本项目的正式训练与评估统一按 **全量行保留 + 按目标 mask 评分** 的契约执行（见 `训练计划_物理参数化与全量预测.md` §2.1、§6）。
- 常量占位 (0.1, 0.01, 99.9) 作为**联合常量状态**参与全部评分与监督；只有当某目标被 `missing_t` 标记（如值为 `-6`）时，该目标不参与该行的评分。
- 该决策在官方评测范围未明确前作为**冻结工作假设**，与 `E0_report.json`、`src/well_folds.json`、`src/score.py` 保持一致。

此外，BIT、CASE 等列在同一口井中多为固定值或 -99999，对模型区分度有限；DEVI/AZIM 单位写法存在不同写法，解析时建议根据工作假设统一处理。

## 六、提交规范

队伍全体成员均可通过工作台完成提交。每支队伍设有**单日提交次数上限（5 次/日）**，达到上限后当日无法再次提交。提交内容包含**代码文件**和**预测结果**，均为 zip 形式。

### 6.1 预测结果提交要求

必须严格按照指定 JSON 结构输出预测文件 `result.json`，**禁止增删、修改任意字段**，打包为 `result.zip` 上传评测。

标准 JSON 格式示例（以官方模板 [`data/测试数据返回结果格式.json`](file:///d:/基于测井曲线的储层参数预测/data/测试数据返回结果格式.json) 为准）：

```json
{
  "modelId": "",
  "modelName": "模型名称",
  "version": "1.0",
  "resultData": [
    {
      "logId": "39dc13c9ca1e4f0eb5f9cb311fe564c3",
      "predictions": [
        {
          "depth": 505.0,
          "POR": 0.1,
          "PERM": 12.35,
          "SW": 0.426
        },
        {
          "depth": 505.1,
          "POR": 0.102,
          "PERM": 13.02,
          "SW": 0.431
        }
      ]
    }
  ]
}
```

结构说明：

- 顶层字段：`modelId`、`modelName`、`version`、`resultData`；
- `resultData`：数组，按井（log）组织预测结果，每项包含：
  - `logId`：井次唯一标识（UUID 格式字符串，对应 `data/train`、`data/test` 下文件名）；
  - `predictions`：该井各深度点预测结果数组，每点含 `depth`、`POR`、`PERM`、`SW` 四个字段，深度采样间隔 0.1 m。

> **注意**：官方 JSON 模板中深度字段为小写 `depth`（赛事文档截图个别处写作大写 `DEPTH`）。两者冲突时**以官方下发模板为准**，提交前请严格按 `data/测试数据返回结果格式.json` 的键名生成，避免字段名大小写不一致导致评测失败。

### 6.2 代码文件提交要求

必须同步提交完整、可独立运行、结果可复现的代码包 `submission_code.zip`。保证复现时能够通过命令行指定数据路径和结果输出路径，**一次性完成数据读取、预处理、模型加载、推理预测及结果文件生成**，无需人工修改代码、手动写入数据或交互式调试。

代码目录结构如下：

```
submission_code/
├── README.md              # 算法说明、运行方式、依赖环境
├── predict.py             # 主预测脚本（必填）
├── requirements.txt       # Python 依赖包列表（必填）
├── config.yaml            # 可选：模型参数或路径配置
├── src/                   # 可选：算法模块代码
│   ├── model.py           # 模型定义/实现
│   ├── feature.py         # 特征工程模块
│   └── utils.py           # 工具函数
├── models/                # 可选：训练好的模型文件或权重
│   ├── model.pkl          # 序列化模型
│   └── scaler.pkl         # 特征缩放器
└── examples/              # 可选：样例输入输出说明
    └── result_example.json
```

### 6.3 README.md 建议包含内容

1. **算法名称**：如“基于时间序列特征融合的测井曲线异常检测模型”；
2. **方法简介**：简述模型架构、特征工程、训练策略、预测逻辑；
3. **运行环境**：列明 Python 版本、全部依赖包及对应版本；
4. **运行命令**：提供可直接复现预测流程的完整执行指令，参数说明如下：
   - `python predict.py --data_dir ./data --output ./result.json`
   - `a. --data_dir`：用于指定测试数据目录或文件路径，评测 data 文件夹下目录结构与赛事下发的原始数据完全一致；
   - `b. --output`：用于指定预测结果保存路径；
5. **文件说明**：标注核心脚本、模型权重、特征处理模块用途。

## 七、评分规则

### 7.1 榜单生成机制

比赛平台依据选手提交结果，自动计算 A 榜得分、B 榜得分：

- **A 榜**：根据 **5 口**测试井数据计算得出，**实时展示**，用于模型调优；
- **B 榜**：根据**全部 10 口**测试井数据计算得出，**赛后统一发布**，用于选手晋级、评奖。

### 7.2 统一客观评价指标

初赛、决赛采用同一套量化打分标准。针对测试集中的 N 个深度点，设 yᵢ 为真实值，ŷᵢ 为模型预测值，为防止分母为零引入极小值 ε。

### 7.3 各项参数准确率定义

**① 孔隙度（POR）准确率 Acc_POR：**

```
              N
Acc_POR = 1/N Σ max(0, 1 − |ŷᵢ − yᵢ| / (0.08 × (|yᵢ| + ε)))
             i=1
```

即：按 8% 相对误差容忍阈值逐点计算线性得分，误差超过阈值截断为 0，再取平均。

**② 渗透率（PERM）准确率 Acc_PERM：**

```
                    N
Acc_PERM = 1/N Σ max(0, 1 − |log₁₀(max(ŷᵢ / yᵢ, ε))|)
                   i=1
```

即：渗透率在数量级范围内变化，采用**对数空间相对误差**；预测与真实相差 10 倍（log 误差为 1）时得分为 0。

**③ 含水饱和度（SW）准确率 Acc_SW：**

```
             N
Acc_SW = 1/N Σ max(0, 1 − |ŷᵢ − yᵢ| / (0.05 × (|yᵢ| + ε)))
            i=1
```

即：按 5% 相对误差容忍阈值逐点计算线性得分，误差超过阈值截断为 0，再取平均。

### 7.4 综合百分制总分

```
Total_Score = (0.3 × Acc_POR + 0.35 × Acc_PERM + 0.35 × Acc_SW) × 100
```

满分 100，PERM 与 SW 权重各 35%，POR 权重 30%。

## 八、复现说明

### 8.1 复现目的

复现验证的目的是保证选手提交的预测结果是由提交代码真实运行而来，确保比赛公平公正。

### 8.2 复现流程

1. 初赛结束后，根据各赛题 B 榜排名由高到低，依次选取拟晋级队伍；
2. 下载队伍最终提交的代码包和预测结果文件；
3. 根据队伍提交的 README.md 中的运行环境说明，配置复现环境；
4. 按照 README.md 中提供的可直接复现预测流程的完整执行指令，运行代码生成预测结果；
5. 执行评分脚本验证得分一致性，比对复现生成的预测结果与选手提交的预测结果。

### 8.3 复现验证标准

1. **代码可独立运行**：无需人工修改代码、手动写入数据或交互式调试操作，即可通过命令行完成完整“训练 + 推理”流程；
2. **结果一致性**：复现生成的预测结果与选手提交的预测结果一致（允许因浮点精度导致的微小差异），建议选手在代码内部固定随机种子；
3. **运行环境可复现**：README.md 或 requirements.txt 中明确列明 Python 版本、全部依赖包及对应版本。

### 8.4 异常处理

1. **复现失败**：包括代码无法正常运行、复现结果与提交结果不一致两种情况，记录失败原因，**取消该队伍的晋级资格**，由后续排名队伍依次递补；
2. **发现作弊行为**：如发现代码与预测结果严重不符、存在数据泄露等作弊行为，**立即取消比赛资格及所有成绩**。

## 九、关键要点速览

| 事项 | 关键信息 |
| --- | --- |
| 数据规模 | 规则描述 90 训练井 + 10 测试井；**本地下发 80 + 10** |
| 文件格式 | 第 1 行列名、第 2 行单位、第 3 行起数据，0.1 m 采样 |
| 预测目标 | POR、PERM、SW 三个连续值回归 |
| 任务输入 | 14 条测井曲线：GR、PE、SP、CAL、AC、DEN、CNL、RXO、RT、DEVI、AZIM、BIT、CASE（+ DEPTH） |
| 标签质量 | 训练集约 2/3 采样点为无效/占位标签（-99999 或常量），须过滤 |
| 晋级门槛 | B 榜总分 > 75 分（基线 75 分） |
| 提交内容 | result.zip（result.json）+ submission_code.zip |
| 单日提交 | 上限 5 次/日 |
| A 榜 / B 榜 | 5 口 / 10 口测试井 |
| 总分公式 | (0.3×Acc_POR + 0.35×Acc_PERM + 0.35×Acc_SW) × 100 |