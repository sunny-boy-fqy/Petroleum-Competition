# v4 依赖（推理 + 训练）
#
# 基础栈由平台镜像预装，**不得 pip 替换**：
#   python==3.11 / torch==2.4.0+cu124 / numpy==1.26.4
# 安装脚本：v4/E0/code/setup_deps.sh（--no-cache-dir + 装完清缓存 + 拒绝触碰 torch）
# 训练侧权威快照：versions/locks/cloud.txt ；推理侧：versions/locks/submit.txt
# 说明文档：docs/image_requirements.md

torch==2.4.0
numpy==1.26.4
pandas==2.2.3
pyarrow==17.0.0
scipy==1.13.1
scikit-learn==1.5.2
einops==0.8.0
onnx==1.16.2
onnxruntime==1.18.1
